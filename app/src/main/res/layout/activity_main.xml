<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    android:id="@+id/l_principal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.anacristina.lectorlibros.MainActivity">

    <!-- Incluímos el layout de la ToolBar en el layout principal y le asignamos un ID que nos permita referenciarlo. -->
    <include
        layout="@layout/toolbar"
        android:id="@+id/toolbar_main">
    </include>

    <TextView
        android:id="@+id/lb_buscar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar_main"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="52dp"
        android:text="@string/ppal_buscar"
        android:textSize="20sp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/lb_titulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="180dp"
        android:text="@string/ppal_titulo" />

    <EditText
        android:id="@+id/txt_titulo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="210dp"
        android:layout_marginStart="30dp"/>

    <TextView
        android:id="@+id/lb_autor"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="280dp"
        android:text="@string/ppal_autor" />

    <EditText
        android:id="@+id/txt_autor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="310dp"
        android:layout_marginStart="30dp"/>

    <Button
        android:id="@+id/bt_buscar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/txt_autor"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="20dp"
        android:text="@string/ppal_buscar" />

</RelativeLayout>